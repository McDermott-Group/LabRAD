TODO LIST
+ figure out how to open everything using the node stuff
- figure out fridge settings
- create the rest of the instruments so we can run this on other fridges
+ make repository
- GGG temp monitoring
- do we want to have a recording checkbox with a time interval?
- select between ADRs/contexts
- try running some of this from dif computers
+ errors not handled nice if instruments are not connected (NoDevicesAvailableError)
- should not be able to mag up when it is warm
+ FAA pill is giving 45 instead of nan
- ADRClient should get settings path from ADRServer

QUESTIONS
+ how can I access the LABRADNODE env variable any time? THISLABRAD should be deleted
- why does the refreshing on a gpib added message not work?
- is it possible to overwrite an existing @setting? ==> yes, but method name and id need to be same

INSTRUCTIONS TO OPEN ADR PROGRAM
open labrad, click run
open node server in 'start' folder
open adr server
open adr client

TUTORIAL STUFF
contexts are passed, and then each context becomes a dictionary to store stuff in
